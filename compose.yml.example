services:
  goviesdeze:
    build: .
    container_name: goviesdeze
    restart: unless-stopped
    environment:
      - LIMITS=8
    ports:
      - 3000:3000
    volumes:
      - ./storage:/storage
      - ./usage.json:/work/usage.json
